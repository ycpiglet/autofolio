schema: agent-runtime-next-session-pointer/v1
updated_at: 2026-06-27T18:13:55+09:00
updated_by: lead-engineer
current_state:
  signal: pass
  score: 96
  summary: >
    Full 12-PR session close-out (#124-#135). #124 UI backlog (TASK-140/173/174).
    #125 TASK-087 BUCKET A (reversible production-readiness code). #126 BUCKET B:
    Supabase autofolio-staging applied (22 tables / 33 RLS / 0 lints). #127-#130
    INIT-MULTITENANT-ENGINE Phases 1-4 complete. #131 governance. #132
    config-gated Postgres adapter (app/database/pg_db.py; DATABASE_URL; default
    SQLite byte-identical; psycopg optional). #133 SQL dialect compat
    (boolean/KST-date/ON CONFLICT portable + supabase/migrations/0004_aux_tables.sql)
    — LIVE-verified via MCP against autofolio-staging. #134 multitenant flag-ON
    readiness: owner-only per-user re-enable/status/toggle endpoints +
    _user_ctx/_user_run_locks TTL/LRU eviction + ghost-lock TOCTOU fix; all
    flag-gated. #135 investor_profile.py PG-readiness — LIVE-verified via MCP
    (investor_profiles upsert + checkin RETURNING on real PG). Full pytest
    1833 passed / 0 failed. DB layer is Postgres-compatible end-to-end and
    live-verified against autofolio-staging. MULTITENANT-FLAG-ENABLE-READINESS.md
    items (a)(b)(d) COMPLETE. Remaining (all Owner-gated): (c) per-user KIS
    credentials (Owner real secrets), live psycopg connection (DB password at
    deploy), Railway/Render backend host (external account), flag-ON Owner
    approval, BUCKET C legal (KIS commercial terms, 비조치의견서, 사업자등록,
    payment/privacy). AUTOFOLIO_MULTI_TENANT_ENABLED default OFF; DATABASE_URL
    default unset (SQLite). can_launch=false. TASK-088 deferred; TASK-069
    scheduled 2026-12-14.
active_work:
  contract: >
    This file is the live pointer for active agents, teams, and panes. When no
    work is actively claimed, current_agents and current_teams stay empty and
    resume.active_task is none.
  current_agents: []
  current_teams: []
  canonical_live_sources:
    - agents/runtime/task_claims/*.json
    - agents/runtime/events/*.jsonl
    - agents/project/NEXT-SESSION-POINTER.yml
    - agents/lead_engineer/STATUS.md
resume:
  first_read:
    - agents/project/NEXT-SESSION-POINTER.yml
    - README.md
    - AGENTS.md
    - CLAUDE.md
    - agents/lead_engineer/STATUS.md
    - agents/lead_engineer/tasks/BACKLOG.md
  active_task: none
  task_set_id: none
  step_index: 0
  step_total: 0
  status_text: >
    12-PR session (#124-#135) complete. Postgres DB layer live-verified. Multitenant
    code complete behind OFF flag. All autonomous code work done — remaining
    items are Owner-gated secrets/decisions. No active task.
  next_actions:
    - Per-user KIS credentials (c): requires real per-user KIS app-key/secret/account — Owner secrets, not autonomous.
    - Live psycopg connection to autofolio-staging: requires DATABASE_URL with DB password — Owner sets at deploy.
    - Railway/Render backend host: external account access — Owner/R3 gate. No autonomous progress.
    - AUTOFOLIO_MULTI_TENANT_ENABLED flag-ON: item (c) + DB password + Railway + Owner explicit safety approval.
    - BUCKET C legal (KIS commercial terms, 비조치의견서, 사업자등록, payment/privacy) — Owner/external, no autonomous progress.
    - Keep TASK-088 deferred until product maturity and presale regulatory clearance.
    - Keep TASK-069 deferred until 2026-12-14 unless Owner explicitly requests early reassessment.
    - Start only on a new explicit Owner request or a newly registered ACT task.
roles:
  accountable: Lead Engineer
  reviewers:
    - QA
    - Independent Auditor
    - Doc Steward
pointers:
  status: agents/lead_engineer/STATUS.md
  backlog: agents/lead_engineer/tasks/BACKLOG.md
  task_index: agents/lead_engineer/tasks/INDEX.md
  compound: agents/lead_engineer/compound_log.md
rules:
  live_work_continuity: >
    Update this pointer and task claim records while work is happening, not only
    at final handoff. It must answer who is working, from which team/pane, on
    which task, in which phase, with what progress, in which worktree, and what
    should happen next.
  repeated_request_api: >
    If the Owner repeats a request or criticism twice, convert it into a
    function, API, script, hook, gate, or explicit task proposal instead of
    leaving it as prose.
  compound_capture: >
    Repeated mistakes or criticisms must produce a Compound entry and an
    executable prevention step when feasible.
  measured_improvement: >
    Use Evaluate -> Propose -> Verify -> Merge. Keep one variable per verified
    change, preserve golden set failures and edge cases, and reserve final
    criteria and merge decisions for Owner.
verification:
  required:
    - python scripts/continuity_contract_gate.py --check
    - python scripts/owner_governance_gate.py --allow-empty-owner-docs
    - python scripts/check_agent_docs.py
  last_known:
    signal: pass
    detail: >
      2026-06-27 12-PR session close-out (#124-#135): #124 UI; #125 TASK-087
      BUCKET A (pytest 1704); #126 BUCKET B Supabase staging (22 tables/33 RLS/0
      lints); #127-#130 INIT-MULTITENANT-ENGINE Phases 1-4 (pytest 1766); #131
      governance; #132-#135 Postgres DB-layer compat + multitenant readiness
      (pytest 1833 passed / 0 failed, live-verified vs autofolio-staging, flag
      OFF). Records closed on chore/pg-readiness-closeout. Pre-existing legacy
      taskset-work-gate failures on main confirmed; no new failures from this
      branch. can_launch=false.

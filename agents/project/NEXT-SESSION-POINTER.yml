schema: agent-runtime-next-session-pointer/v1
updated_at: 2026-06-27T11:24:43+09:00
updated_by: lead-engineer
current_state:
  signal: pass
  score: 94
  summary: >
    TASK-087 BUCKET A (reversible production-readiness code layer) merged to
    main as PR #125: A1 central feature-flags (default-OFF), A2 auto-exec/
    recommendation/advice OFF-locks (kill-switch first), A3 SSO role hardening
    (AUTOFOLIO_OWNER_EMAIL fail-closed), A4 Supabase migration files+RLS,
    A5/A6 metadata-only secret-store adapter + presence-only env loader, A7
    deploy config + preflight CI. pytest 1704 passed; per-task + Opus
    whole-branch reviews Approved. TASK-087 BUCKET B DB tier (PR #126,
    feat/task087-staging-evidence, open): new Supabase project autofolio-staging
    (rpdophwfgrwctaochewf, ap-northeast-2) created; migrations 0001-0003 applied
    — 22 tables / 33 RLS policies / 0 RLS-without-policy / 0 security-advisor
    lints. Evidence: agents/project/MEMBERSHIP-STAGING-DEPLOY-EVIDENCE.md.
    INIT-MULTITENANT-ENGINE registered (deliberate trading-engine user_id
    isolation; app-to-Supabase wiring depends on it). Remaining: app-tier deploy
    (Railway backend + INIT-MULTITENANT-ENGINE wiring + Vercel web) and BUCKET C
    legal (KIS commercial terms, 비조치의견서, 사업자등록, payment/privacy).
    can_launch=false. Earlier: UI backlog TASK-140/173/174 + feat/ui-backlog-2026-06-26
    merged (#124). TASK-088 deferred until product maturity; TASK-069
    scheduled 2026-12-14.
active_work:
  contract: >
    This file is the live pointer for active agents, teams, and panes. When no
    work is actively claimed, current_agents and current_teams stay empty and
    resume.active_task is none.
  current_agents: []
  current_teams: []
  canonical_live_sources:
    - agents/runtime/task_claims/*.json
    - agents/runtime/events/*.jsonl
    - agents/project/NEXT-SESSION-POINTER.yml
    - agents/lead_engineer/STATUS.md
resume:
  first_read:
    - agents/project/NEXT-SESSION-POINTER.yml
    - README.md
    - AGENTS.md
    - CLAUDE.md
    - agents/lead_engineer/STATUS.md
    - agents/lead_engineer/tasks/BACKLOG.md
  active_task: none
  task_set_id: none
  step_index: 0
  step_total: 0
  status_text: >
    TASK-087 BUCKET A merged (#125). BUCKET B Supabase staging applied; PR #126
    open on feat/task087-staging-evidence. INIT-MULTITENANT-ENGINE registered.
    No active task — next is merge #126, then app-tier + BUCKET C (Owner/external).
  next_actions:
    - Merge PR #126 (feat/task087-staging-evidence) — BUCKET B Supabase staging evidence.
    - After merge: proceed with INIT-MULTITENANT-ENGINE for app-to-Supabase wiring (trading-engine user_id isolation).
    - App-tier deploy: Railway backend host + INIT-MULTITENANT-ENGINE wiring + Vercel web deploy.
    - BUCKET C legal is Owner/external — KIS commercial terms, 비조치의견서, 사업자등록, payment/privacy. No autonomous progress possible.
    - Keep TASK-088 deferred until product maturity and presale regulatory clearance.
    - Keep TASK-069 deferred until 2026-12-14 unless Owner explicitly requests early reassessment.
    - Start only a new explicit Owner request or a newly registered ACT task.
roles:
  accountable: Lead Engineer
  reviewers:
    - QA
    - Independent Auditor
    - Doc Steward
pointers:
  status: agents/lead_engineer/STATUS.md
  backlog: agents/lead_engineer/tasks/BACKLOG.md
  task_index: agents/lead_engineer/tasks/INDEX.md
  compound: agents/lead_engineer/compound_log.md
rules:
  live_work_continuity: >
    Update this pointer and task claim records while work is happening, not only
    at final handoff. It must answer who is working, from which team/pane, on
    which task, in which phase, with what progress, in which worktree, and what
    should happen next.
  repeated_request_api: >
    If the Owner repeats a request or criticism twice, convert it into a
    function, API, script, hook, gate, or explicit task proposal instead of
    leaving it as prose.
  compound_capture: >
    Repeated mistakes or criticisms must produce a Compound entry and an
    executable prevention step when feasible.
  measured_improvement: >
    Use Evaluate -> Propose -> Verify -> Merge. Keep one variable per verified
    change, preserve golden set failures and edge cases, and reserve final
    criteria and merge decisions for Owner.
verification:
  required:
    - python scripts/continuity_contract_gate.py --check
    - python scripts/owner_governance_gate.py --allow-empty-owner-docs
    - python scripts/check_agent_docs.py
  last_known:
    signal: pass
    detail: >
      2026-06-27 TASK-087 BUCKET A+B session: BUCKET A merged (#125) with pytest
      1704 passed + per-task + Opus whole-branch Approved; BUCKET B Supabase
      staging autofolio-staging applied (22 tables/33 RLS/0 lints), PR #126 open.
      App-tier deploy and BUCKET C legal remain env-blocked/Owner-external —
      honestly recorded; can_launch=false. Gates: continuity_contract_gate pass,
      check_agent_docs 0 errors, owner_governance_gate --allow-empty-owner-docs
      pass (no new taskset-work-gate failures added).

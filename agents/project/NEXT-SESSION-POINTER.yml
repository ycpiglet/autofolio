schema: agent-runtime-next-session-pointer/v1
updated_at: 2026-06-27T15:28:04+09:00
updated_by: lead-engineer
current_state:
  signal: pass
  score: 94
  summary: >
    Full session close-out: 7 PRs merged this session. #124 UI backlog
    (TASK-140/173/174). #125 TASK-087 BUCKET A (reversible production-readiness
    code layer). #126 TASK-087 BUCKET B: Supabase autofolio-staging applied
    (22 tables / 33 RLS / 0 advisor lints). #127-#130 INIT-MULTITENANT-ENGINE
    Phases 1-4 ALL COMPLETE: Phase 1 = repo query-scoping (14 methods); Phase 2
    = per-user risk context / circuit-breaker isolation; Phase 3 = per-user
    engine context pool + per-user run lock + per-user engine_state
    (auto_trading/kill-switch/both circuit-breakers per-user; consecutive-failures
    cross-user hole closed); Phase 4 = cross-user e2e isolation tests (6 layers)
    + flag-OFF characterization (byte-identical proven) +
    MULTITENANT-FLAG-ENABLE-READINESS.md. Full pytest 1766 passed / 0 failed.
    AUTOFOLIO_MULTI_TENANT_ENABLED flag stays OFF in all environments.
    Remaining: (1) SQLite→Supabase Postgres backend swap, (2) Railway backend
    host (external account — Owner/R3 gate), (3) flag-ON readiness items +
    Owner approval (MULTITENANT-FLAG-ENABLE-READINESS.md), (4) BUCKET C legal
    (KIS commercial terms, 비조치의견서, 사업자등록, payment/privacy).
    can_launch=false. TASK-088 deferred until product maturity; TASK-069
    scheduled 2026-12-14.
active_work:
  contract: >
    This file is the live pointer for active agents, teams, and panes. When no
    work is actively claimed, current_agents and current_teams stay empty and
    resume.active_task is none.
  current_agents: []
  current_teams: []
  canonical_live_sources:
    - agents/runtime/task_claims/*.json
    - agents/runtime/events/*.jsonl
    - agents/project/NEXT-SESSION-POINTER.yml
    - agents/lead_engineer/STATUS.md
resume:
  first_read:
    - agents/project/NEXT-SESSION-POINTER.yml
    - README.md
    - AGENTS.md
    - CLAUDE.md
    - agents/lead_engineer/STATUS.md
    - agents/lead_engineer/tasks/BACKLOG.md
  active_task: none
  task_set_id: none
  step_index: 0
  step_total: 0
  status_text: >
    INIT-MULTITENANT-ENGINE Phases 1-4 complete (PRs #127-#130, pytest 1766
    passed). All 7 PRs from this session merged (#124-#130). Flag stays OFF.
    No active task — next autonomous work requires new Owner request or ACT task.
  next_actions:
    - SQLite→Supabase Postgres backend swap: app-layer connection wiring to autofolio-staging. Can be drafted autonomously but requires Owner/R3 for env secrets.
    - Railway/Render backend deploy: external account access needed — Owner/R3 gate. No autonomous progress.
    - AUTOFOLIO_MULTI_TENANT_ENABLED flag-ON: clear MULTITENANT-FLAG-ENABLE-READINESS.md items, then Owner explicit approval required.
    - BUCKET C legal (KIS commercial terms, 비조치의견서, 사업자등록, payment/privacy) — Owner/external, no autonomous progress.
    - Keep TASK-088 deferred until product maturity and presale regulatory clearance.
    - Keep TASK-069 deferred until 2026-12-14 unless Owner explicitly requests early reassessment.
    - Start only on a new explicit Owner request or a newly registered ACT task.
roles:
  accountable: Lead Engineer
  reviewers:
    - QA
    - Independent Auditor
    - Doc Steward
pointers:
  status: agents/lead_engineer/STATUS.md
  backlog: agents/lead_engineer/tasks/BACKLOG.md
  task_index: agents/lead_engineer/tasks/INDEX.md
  compound: agents/lead_engineer/compound_log.md
rules:
  live_work_continuity: >
    Update this pointer and task claim records while work is happening, not only
    at final handoff. It must answer who is working, from which team/pane, on
    which task, in which phase, with what progress, in which worktree, and what
    should happen next.
  repeated_request_api: >
    If the Owner repeats a request or criticism twice, convert it into a
    function, API, script, hook, gate, or explicit task proposal instead of
    leaving it as prose.
  compound_capture: >
    Repeated mistakes or criticisms must produce a Compound entry and an
    executable prevention step when feasible.
  measured_improvement: >
    Use Evaluate -> Propose -> Verify -> Merge. Keep one variable per verified
    change, preserve golden set failures and edge cases, and reserve final
    criteria and merge decisions for Owner.
verification:
  required:
    - python scripts/continuity_contract_gate.py --check
    - python scripts/owner_governance_gate.py --allow-empty-owner-docs
    - python scripts/check_agent_docs.py
  last_known:
    signal: pass
    detail: >
      2026-06-27 full session close-out: #124 UI; #125 TASK-087 BUCKET A (pytest
      1704); #126 BUCKET B Supabase staging (22 tables/33 RLS/0 lints); #127-#130
      INIT-MULTITENANT-ENGINE Phases 1-4 (pytest 1766 passed / 0 failed, flag OFF).
      Records closed on chore/multitenant-closeout. Gates pending run at commit
      time — pre-existing legacy taskset-work-gate failures on main confirmed;
      no new failures from this branch. can_launch=false.

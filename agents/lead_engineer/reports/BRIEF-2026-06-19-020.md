# BRIEF-2026-06-19-020 — 실계좌 라이브 스모크 + 매수가능 버그수정 + 모의 UI 검증

작성: Lead Engineer · 2026-06-19T17:29:42+09:00 · ref TASK-111 / TASK-112

Bottom Line: 정규장 중 Owner 요청으로 모의→실계좌 최소 주문 경로와 UI 매매 흐름을 실제 검증했고, 그 과정에서 매수를 전면 차단하던 실버그를 찾아 고쳤다. 추가 UI+KIS 복합 테스트는 장 마감으로 내일 런북(TASK-112)으로 이관하고 스케줄러를 걸었다.

## Signal
- 실계좌(prod) 최소 주문: `004870` 시장가 5주 매수→매도→1주 지정가 취소 → overall **pass**, final position delta 0, open-like 0. (캡 ≤5주·≤5,000원)
- 모의(paper) 주문 경로: `005930` 대형주 시장가 매수/매도 체결, delta 0 → PASS.
- 버그수정 2건: (1) 깨진 스모크 대본 헬퍼 복구, (2) `get_buying_power`가 `psbl_qty`만 봐서 모의에서 항상 0이던 것을 `nrcvb_buy_qty`/`max_buy_qty` fallback으로 수정 + 회귀 테스트. unit 10 passed.
- 모의 UI(격리 paper 인스턴스): 로그인→조건등록→새로고침 지속까지 유저처럼 조작 통과.

## Insight
- 모의 서버는 대형주 위주 제한 유니버스 → 저가 종목은 모의 매매불가(설계 한계). 저가 화이트리스트로는 모의 PASS 불가.
- Next `rewrites`는 빌드 시점 고정 → `API_INTERNAL_URL`을 빌드에 넣어야 백엔드 연결됨.
- dev는 헤드리스/프록시에서 HMR 실패로 하이드레이션 깨짐 → 프로덕션 빌드로 검증(메모리 원칙과 일치).
- `scripts/now.py`가 OS 시각과 어긋남 → OS 클럭 기준 사용.

## Decision
- 오늘 작업 TASK-111(완료)로 기록. 추가 UI+KIS 복합 테스트는 TASK-112(내일 런북, 대기)로 이관.
- 내일 정규장 시작(09:05 KST) 스케줄 트리거 생성 — paper 우선, 실주문은 Owner 확인 + 캡 스크립트로만.
- 미커밋 변경(kis_client/kis_paper_order_smoke/test/pp.py)은 동시 세션 변경 혼재로 커밋 보류.
